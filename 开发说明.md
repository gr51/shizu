# 噬祖 · 开发说明

> 本文档基于**当前代码实际状态**编写（2026-08-23 全量核对），取代此前累积的旧版开发说明。
> 原始需求源档在 `噬祖/` 目录；本文只描述「游戏现在是什么、怎么跑、怎么改」。

---

## 一、这是什么游戏

《噬祖·诸天噬灵》——像素武侠风**类吸血鬼幸存者**割草 Roguelite，题材致敬《无限恐怖》《王牌进化》的无限流：

- 玩家（巢灵）进入 12 个位面裂缝，割草、吞噬基因、三选一构筑、击破位面之主
- **基因锁**：首次通关位面永久激活路线（十选一互斥），六段成长 + 组合技
- **支线协议**：每局随机一条可选任务（歼灭/收割/速通/猎头），完成得额外基因
- **完成度评价**：多因子 S+→D 评级，按评级发放结算加成
- **虫巢强化**：局外永久升级（体质/利齿/抉择/断绝/贪食/残命/商路/先祖）
- **危机事件**：S2-S4 每 38s 随机触发（陨石雨/狂暴化/虫巢震荡），预警圈可躲
- **无尽模式**：通关诸天之心后解锁，深渊层递进

双端：`web/`（DOM+Canvas 原型，主验证场）与 `shizu-cocos/`（Cocos Creator 3.8 工程）共用同一 core。

---

## 二、命令

```bash
npm run serve       # → http://localhost:8123/web/   可玩（WASD 移动 · 空格吞噬 · Shift 闪避）
npm test            # 177 项单元测试（core/data 逻辑 + 九条红线）
npm run typecheck   # tsc 检查 Cocos 脚本层
npm run smoke       # Cocos 组件层冒烟：真实驱动 GameRoot 打完整局（含 20+ 流断言）
npm run play:flows  # Web 交互流守护：配置透传/购买/无尽（12 断言，需先 serve）
npm run probe       # 纯 Node 压测：帧率 / 击杀 / 平衡基线
npm run shots       # 界面截图巡检（需 serve）
npm run audit:art   # 资产完整性体检
npm run audit:reach # 内容到达率体检（玩家实际见得到多少内容）
npm run plane:new <id> <中文名> <codex>   # 新位面脚手架
```

交付前必跑：`npm test && npm run typecheck && npm run smoke`（= `npm run verify`）。
Web 交互改动追加 `npm run play:flows`。

---

## 三、架构

```
shizu/
├── shizu-cocos/assets/scripts/
│   ├── core/     唯一真源：零 DOM、零 cc 依赖的 plain JS
│   │   ├── battle.js      实时战斗（敌人/机制/危机/特效队列/连击）
│   │   ├── run.js         局状态机（三选一/黑市/结算/评级/支线）
│   │   ├── dungeon.js     副本生成（阶段时间轴/无尽层）
│   │   └── balance.js     战力/D/动态难度/基因锁倍率
│   ├── data/     纯配表
│   │   ├── planes.js            位面静态数据（12 位面）
│   │   ├── planeModules.js      ★ 位面插件注册表（九切面门面）
│   │   ├── routes.js            十路线基因锁定义
│   │   ├── synergies.js         构筑共鸣 ×22
│   │   ├── riftMods.js          裂缝变异 ×8
│   │   ├── shopItems.js         黑市商品 ×13（含权衡型）
│   │   ├── eliteAffixes.js      精英词缀 ×8
│   │   ├── sideQuests.js        支线协议 ×4
│   │   ├── crises.js            危机事件 ×3
│   │   └── achievements.js      行为成就 ×14
│   ├── game/     Cocos 组件层（GameRoot.ts 等）
│   └── types/cc.d.ts    编辑器外类型桩
├── web/          DOM 原型壳（battleScreen/renderer/input/audio/view/lobby/cards）
├── tests/        177 项 node:test
├── tools/        验证与生成工具（smoke/play/play-flows/probe/shots/ai-art/*）
└── 噬祖/         原始策划文档（需求源，不改）
```

**分层规则**：core/data 是唯一真源；web 与 Cocos 是两套独立 UI 共用 core。
给 core 加新状态时**必须同时改两套 UI** 并让 smoke 覆盖。

---

## 四、位面插件化

**一个位面 = 一个可插拔内容包**。全部切面收拢在 `data/planeModules.js`：

| 切面 | 内容 | 消费端 |
|---|---|---|
| entry | 开场诗 / 主题 / Boss 机制词 | 裂缝弹窗、开局简报 |
| enemies | 5 阶段表×2 只 / 远程集 | 刷怪与渲染 |
| mechanic | 常驻规则 + 招牌事件（12 位面互异） | mechanicsTick + 双端特效 |
| boss / skillRoutes | Boss 映射 / 技能来源路线 | 结算激活 / 三选一池 |
| art | 地砖/背景/角色形象路径约定 | 渲染层与生成管线 |
| audio | BGM 调性键 + 环境音层标记 | startBgm |
| crises | 允许的危机子集（null=全部） | crisisTick 掷骰过滤 |
| drops | 位面主题传承加权池 | relic 掷骰 |

统一入口 `getPlaneModule(id)`；外部系统不直接摸散表。

### 加新位面

```bash
npm run plane:new xiuluo 修罗界 13    # 产出粘贴片段 + 13 条资产 prompt 清单
# 填 planes.js 与 planeModules.js → generate.mjs --plane 出资产
npm test                              # tests/planeModules.test.mjs 五项校验当场把关
```

---

## 五、玩法系统清单（当前全部已接线）

| 系统 | 要点 |
|---|---|
| 支线协议 ×4 | 歼灭/收割/速通/猎头；开局简报+HUD 进度条+结算奖励 |
| 完成度评价 | 六因子计分 → S+/S/A/B/C/D；S+/S/A 发基因加成 |
| 击杀连击 | 1.5s 窗口累积；里程碑阶梯称号+基因直入库存；HUD 计数器 |
| 变体攻击分化 | walker/charger/tank/spitter/bomber 五种行为与剪影各异 |
| 位面招牌事件 ×12 | 每位面周期性可辨事件（金光普照/尸潮/剑气纵横…）+ 专属音效 |
| 危机事件 ×3 | 预警→生效→解除；crises 子集按位面过滤 |
| 锻造/合成/强化 | 双端可用；uid 防自吞 |
| 黑市 v2 | 内联购物卡；权衡型商品（狂暴激素等） |
| 行为成就 ×14 | 三态页面 + 进度提示 |
| 构筑共鸣 ×22 | attr/skill/mech 三系 need；技能局可触发 |

---

## 六、已知边界（当前真实状态）

1. **Cocos 音频缺失**：web 的程序化音频依赖 WebAudio；Cocos 需平台适配专项。
2. **Cocos 特效为占位**：战场实体用 Sprite 池 ✓，但机制特效（激光/闪电/招牌事件）仅 web 绘制；Cocos 端靠叙事 toast 文字播报补偿。
3. **PRD 稀有度裁定待定**：提案 C′（退出 PRD+硬保底 500）vs C，见 git 历史 4.3 决策备忘。
4. **shanhai/jijia 地砖**：audit 大小阈值启发式误报，视觉模型确认可平铺。
5. **变体行为再分化**：移动层改动会稀释「成海」密度（horde 基线实测红过两回），需配套刷怪重校，勿单独动。

---

## 七、验证体系

```
npm test            177 项：九条红线 + 全系统单元
npm run smoke       Cocos 真流程：大厅→裂缝配置(9断言)→整局→结算→落盘
                    + 无尽撤离流(6) + 锻造合成强化链(5) + 叙事toast(2)
npm run play:flows  Web 流：配置透传/购买/无尽 12 断言
```

**教训固化**：
1. 编译通过 ≠ 功能正确——交互流必须运行时验证（配置清空 Bug、锻造模板损伤均由此抓获）
2. 平衡敏感改动必须带隔离实验数据（蛇形逼近两次打红 horde 基线后回滚）
3. 构造器不消耗 rng——meta 层随机用确定性选取，否则重签全部轨迹

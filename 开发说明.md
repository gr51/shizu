# 开发说明

> 本文件是这个仓库唯一的开发文档。
> `噬祖/*.md` 那八份是**策划文档**（需求源），本文件是**实现侧**的全部说明：
> 架构、命令、红线、对策划文档的裁定、美术管线、排查工具、待办。
> （原先散落的 `重建说明.md` / `实时战斗落地计划.md` / `横屏改造说明.md` /
> `美术提示词-像素世界资产图鉴.md` 已并入本文，不再单独维护。）

策划文档的可运行实现。**核心逻辑只有一份**，位于 Cocos 工程内，
网页原型与测试都直接引用它 —— 不存在「网页版一套、Cocos 一套」的复制品。

```
shizu/
├── 噬祖/*.md                       八份策划文档（需求源）
├── 开发说明.md                     ← 你在读的这份
├── shizu-cocos/                    Cocos Creator 3.8 工程（产品）
│   ├── assets/scenes/Main.scene    Canvas + Camera + GameRoot
│   ├── assets/scripts/
│   │   ├── core/  *.js             ★ 唯一真源：零 DOM、零 cc 依赖
│   │   ├── data/  *.js             ★ 纯配表
│   │   ├── platform/storage.ts     Web / 微信 / 抖音 存档适配
│   │   └── game/    *.ts           Cocos 组件层（横屏 960×640，程序化绘制）
│   ├── assets/art/                 美术源（AI 生成 + 像素化）
│   ├── assets/resources/art/       Cocos 运行时副本（与上面同名同内容）
│   └── types/cc.d.ts               编辑器外类型检查用的声明桩
├── tests/                          121 项，测的就是 core/ + data/
├── tools/                          验证与排查工具（见第六节）
└── web/                            DOM 原型壳（验证与手感调试用，不进包）
```

---

## 一、命令

```bash
npm run verify      # 交付前必跑：test + typecheck + smoke
npm test            # 121 项单元测试（core/ + data/）
npm run typecheck   # tsc 检查 Cocos 脚本层
npm run smoke       # 在 Node 里真的实例化 GameRoot 打完一局

npm run serve       # → http://localhost:8123/web/   **可玩**（WASD 移动，自动索敌）
npm run play        # 无头浏览器真的按键打完一局（需先 serve）
npm run shots       # ★ 界面截图自查（需先 serve）—— 见第六节
npm run probe       # 纯 Node 压测实时战斗：帧率 / 击杀 / 平衡
npm run playtest    # 玩法内核体检：升级节奏 / 滚雪球曲线 / 压力曲线 / 动词清单
npm run sim 400     # 400 局成长曲线模拟

npm run audit:art       # 资产完整性体检（缺图/坏地砖）
npm run audit:build     # 多局 Build 深度体检（三选一构成）
npm run audit:enemy     # 敌人独有行为触发率
```

### 各层验证分别证明什么

| 命令 | 证明 | 证明不了 |
|---|---|---|
| `npm test` | 九条红线 + 割草定位 + 战力收益方向 | UI 是否可用、**内容是否真的进得了池** |
| `npm run smoke` | Cocos 组件层能建树、切界面、跑完整局、落盘 | 像素渲染、布局是否好看 |
| `npm run play` | 真实浏览器里按键操作、实时战斗打完一局 | 好不好看 |
| `npm run shots` | **好不好看**、控制台报错、资源 404 | 手感 |
| `npm run probe` | 实时战斗的帧率与平衡（10 万帧/秒纯逻辑） | 真人手感 |
| `npm run audit:*` | 资产/内容/行为**是否真的生效** | 数值是否好玩 |

**都不能替代在 Cocos Creator 里预览一次**（本环境无编辑器）。
第一次在编辑器打开时要做的事见 `shizu-cocos/README.md`。

> ⚠ **测试全绿不等于东西是好的。** 这个仓库出过两次教训：
> 109 项测试全绿时，首个位面的地板铺满的是一张矮人立绘、敌人全是色块；
> 同样全绿时，`data/skills.js` 里的 60 个技能一个都进不了三选一池。
> 单元测试测的是逻辑分支，覆盖不到「难看」和「内容没接上」。
> 所以第六节那些工具不是可选项。

---

## 二、九条编码红线与守护它们的测试

出自《噬祖-开发实现指南》十四章。测试是逐条写的证伪测试，不是覆盖率填充。

| 红线 | 内容 | 测试 |
|---|---|---|
| 1 | 位面不设固有强度，禁止按位面手写强度表 | 12 位面同 D 下敌人数值必须相同；模板值不得流入战斗数值 |
| 2 | D 在开副本时快照 | 局内穿满传说装备后 D 与敌人数值不得变 |
| 3 | 不匹配位面零技能，但装备照掉（×1.5） | 5000 次属性通道三选一不得漏出技能；掉率比值 ≈1.5 |
| 4 | 互斥权重 ×0 必须真生效 | 激活渡劫后 10000 次抽取不得出现尸海/共生巢 |
| 5 | dynFactor 恒钳制 [0.70, 1.50] | 连胜 200 / 连败 200 / 混合 5000 次均不越界 |
| 6 | 存档原子写 | 整局战斗零落盘，finalize 恰好写一次 |
| 7 | 首进固定机关城 | totalRuns=0 时 1000 次必为机关城；之后不得再有固定分支 |
| 8 | 隐藏技能唯一 + 刻印不可被顶 | 重复获得被拒；刻印槽拒绝局内技能 |
| 9 | 装备加成必须抬 D | 穿装备后战力 ×1.3125，D 同步上升 |

---

## 三、架构决策

### 3.1 唯一真源：core/ 与 data/ 是 plain JS

- `core/` + `data/` 是 **plain JS + JSDoc**，Cocos 与浏览器都能直接吃，无需构建。
  这样做是为了让「唯一真源」这件事没有例外 —— 一旦分成 .ts / .js 两份就会漂移，
  而这个仓库上一版正是死于三套并行实现。
- `game/*.ts` 只在 Cocos 里跑，用 TypeScript 是为了装饰器（`@ccclass`）。
- `web/` 不进包，只是验证工具。

> ⚠ **两套 UI 会漂移，这是这个架构的真实代价。**
> `web/` 和 `shizu-cocos/assets/scripts/game/` 是两份独立的 UI 实现，共用一个 core。
> 已经发生过：`core/run.js` 加了 `RunState.SHOPPING`（裂缝黑市）并接进了 web 版，
> 但没人动 `GameRoot.ts` —— 而 `RealtimeRun.update()` 对任何非 FIGHTING 状态直接返回，
> 于是 Cocos 版在第一次阶段转换时整局冻死。
> **给 core 加 RunState 时，必须同时改两套 UI**，并让 `npm run smoke` 覆盖到新状态。

### 3.2 横屏（960×640）

由竖屏 640×960 改为横屏。依据：割草品类需要更宽的视野来「看见怪海」，
与参考项目 Slash The Hordes（Cocos Creator 3.8 开源割草）一致。
本作「单摇杆 + 自动索敌」下，横屏无需牺牲单手操作。

涉及三处：`settings/v2/packages/project.json`（设计分辨率 / fit-width / landscape）、
`game/UiKit.ts` 的 `DESIGN`、`game/GameRoot.ts` 的布局坐标。
策划文档《整体策划 1.1》原文写「竖屏」，这是**实现侧裁定**，与红线无关，属品类手感决策。
要回竖屏只改这三处，`core/` 零改动 —— 它不感知分辨率。

### 3.3 实时战斗：碰撞用距离检测，不用物理引擎

`core/battle.js` 的 `RealtimeRun` 是**唯一**的战斗实现：单摇杆 + 自动索敌 +
接触伤害 + 无敌帧 + 基因吸取。网页与 Cocos 共用这一份，各自只提供渲染与输入。
（原型期的回合制 `combatModel.js` 已随实时战斗落地整体删除。）

架构借鉴 Slash The Hordes 的 `gameTick(dt)` 系统循环、输入抽象、环形刷怪、
拾取吸附；**机制全部用噬祖自己的**（基因锁 / 三选一 / 掉落 / D 值快照 / 九条红线）。

碰撞没用 Cocos 2D 物理，用圆形距离检测：割草核心是「扫一片」不是精确碰撞，
同屏 60 单位下距离检测更稳，也不破坏「纯程序化、无 prefab」的架构。
O(n²) 在 60 只时可接受；同屏上限拉高时再上 Spatial Hash。

---

## 四、对策划文档的裁定

文档内部有冲突或缺口的地方，都在代码里就地注明。以下是完整清单。

### 4.1 已裁定

| # | 冲突 | 裁定 |
|---|---|---|
| 1 | 敌人基准有两张互相冲突的表 | 红线 1 禁止逐位面强度表，取平衡表三章的通用基准。实测让 12 个位面的新手通关率从 0%~14.7% 收敛到 10.7%~17.7% |
| 2 | 硬保底整层缺失 | 整体策划 4.5 要求 PRD 与硬保底两层并列，已补 `PITY_CAP` |
| 3 | 基因→永久成长换算率文档缺失 | 取 600 基因/次（由产出速率反推） |
| 4 | 割草定位重标 | 阶段是时间轴不是离散波次；小怪基准 20/3 → 5/2；击杀量 40 只/局 → 1477 只/局 |
| 5 | 基因锁曾是纯负收益 | 每段 +2% 战力抬 D 却不给战斗力，实测通关率 61% → 0%。改为「解锁即生效、不占技能槽」，锁进 `tests/powerSources.test.mjs` |
| 6 | 两处笔误 | `gaoda` 的 route 写成组名 `'gangtie'`，按平衡表取 `'jijia'`；`tiangong` 是被动却建议刻主动槽，照文档保留并加注释 |
| 7 | **技能通道给哪些段位** | 见下方 4.2 —— 这条是这个仓库迄今最严重的一次「文档与实现互相矛盾导致整个系统死掉」 |

### 4.2 技能池曾经可证明恒为空集（重点）

`core/run.js` 的 `equipGeneLockSkills()` 与 `core/upgrade.js` 的 `skillPool()`
曾经用**同一个谓词** `skill.lv <= geneLocks[route]`：

- 前者在开局把所有满足条件的技能发放，并写进 `learnedSkills`
- 后者收同一批技能，再排除「已学过的」

两个集合完全相同、前者先执行 —— **交集恒为空**。
`data/skills.js` 里 10 条路线 × 6 段共 60 个技能，从来没有任何一局能选到。

取证（`npm run audit:build`）：连打 8 局，**59 次三选一、177 个选项，技能类 0 个**。

而当时 109 项单测全绿 —— 因为红线 3 那条测试传的是空 `learnedSkills`，
**那个状态在真实游戏里根本不存在**。这是「测试测了一个不可能的状态」的典型。

**裁定**：基因锁 = 跨局永久解锁（开局自动生效、不占技能槽，维持原策划裁定）；
三选一 = 本局提前够到「已解锁段位之上、`SKILL_REACH`(=2) 段以内」的段位，打完不保留。
两者不再抢同一批内容，也保留了原意里「不许一步够到终极技」的节奏约束。
改后技能通道局稳定出 2~3 个技能选项。

回归测试：`tests/channels.test.mjs`「模拟开局自动发放后，技能池仍然出得来货」。
**新增任何「开局自动发放」类机制时，必须同步检查它会不会再次清空某个池。**

### 4.3 待策划裁定（两条，都只改常量不动逻辑）

**A. PRD 参数与稀有度目标数学上不可兼容**

《数值平衡表》7.4 给的 step，与 7.3 / 4.8 声称的稀有度目标偏差 3~8 倍。
根因是线性 PRD 的期望次数与硬保底点死锁：`期望 ≈ 0.5 × √(2π / step)`。
要让隐藏技能期望落在 500 次，需硬保底点约 **159,000 次** —— PRD 已形同虚设。
**「0.1% 基础概率」+「PRD 递增」+「期望 500 次」三者无法同时成立。**

| 方案 | 做法 | 后果 |
|---|---|---|
| A（当前落地） | 维持 7.4 的 step | 隐藏技能约 61 局出一个，与「禁忌级」定位冲突 |
| B | 基础概率抬到 ≈0.4%，step ≈1.6e-3 | 期望 ≈500 次，PRD 仍有效；改了 4.8 表头的 0.1% |
| C（倾向） | 隐藏技能退出 PRD，只用硬保底 | 期望 1000 次，硬保底 1500 兜底；最贴合「极稀有」手感，且不改任何已公布数字 |

**B. 后期难度旋钮行程不够**

毕业号（6 路线满段 + 永久 +378% + 6 槽全传说，战力 ≈21）需要 dynFactor ≈3.0（中等）
才有挑战，而红线 5 钳死在 **1.50** —— 行程差约一倍。
根因：基因锁段位的战斗价值随叠加**超线性**增长（AOE 相乘），而战力公式按线性 +2%/段 计。

| 方案 | 做法 | 代价 |
|---|---|---|
| A | 放宽 dynFactor 上限 1.50 → 3.00 | 要改红线 5 |
| B | 基因锁战力权重改为递增（Lv1-2 +2% / Lv3-4 +4% / Lv5-6 +8%） | 平衡表八章要改 |
| C（倾向） | 用无尽模式承接后期（4.4 已有该系统） | 不改数值，但要把无尽模式做出来 |

---

## 五、美术管线

### 5.1 生成

`tools/ai-art/` 是 AI 出图 + 像素化管线。API 走环境变量，**密钥不入库**：

```bash
SHIZU_IMAGE_API_URL=<openai 兼容的 images/generations 端点> \
SHIZU_IMAGE_API_KEY=<key> \
SHIZU_IMAGE_MODEL=<模型名> \
node tools/ai-art/generate.mjs --plane jiguan     # 单一位面（27 张）
npm run art:floor -- jiguan --variants=3          # 只重做地砖，出 3 张候选
npm run art:tile -- <png...>                      # 地砖平铺预览
```

产物同时写入 `assets/art/`（美术源）与 `assets/resources/art/`（Cocos 运行时）。
按 prompt 哈希做磁盘缓存，相同 prompt 不重复烧钱。

### 5.2 三个坑

1. **`generate.mjs` 是全量覆盖的。** 它顶层的 `main()` 现在有 `import.meta.url` 守卫，
   直接执行才会跑。但它一旦跑起来就会覆盖该位面的全部 27 张资产 ——
   **动美术前先确认工作区干净、能 `git checkout` 回退**。
   （曾经误触发一次，把调好的玩家立绘覆盖成了卡通虫子。）

2. **地砖单看一张会骗人。** 判断标准是**平铺后**的样子，不是单张好不好看。
   用 `npm run art:tile` 按渲染层同样的镜像规则拼 4×3 再看。
   好地砖的要求：低对比、细节均匀、**没有大的居中主图案** ——
   有焦点图案的砖铺开就是棋盘壁纸。
   提示词必须显式写 `NO large central motif, NO focal point, LOW CONTRAST`，
   以及 `NO characters / people / creatures` —— 不写的话模型会当成角色 sprite 画张立绘出来。
   （`floor_jiguan.png` 曾经就是一张矮人立绘被平铺满全场，而且是首进教学位面。）

3. **缺图不会报错。** `web/src/game/assets.js` 用 `img.onerror` 静默 `resolve(null)`，
   渲染层退化成色块。所以缺图只能靠 `npm run audit:art` 和 `npm run shots` 的 404 计数发现。

### 5.3 命名契约

渲染层 `renderer.js` 的取图顺序是 `e.sprite || spriteBase(kind, variant, planeId)`：

- 阶段表（`battle.js` 的 `MINION_SPRITE_BY_STAGE`）里有的位面 → `e.sprite` 一定有值，
  用 `units/{sprite}_{walk0..3|atk0..2|death}.png`
- 阶段表里**没有**的位面（目前只有 `jiguan`）→ 回落到 `units/minion_{variant}_{plane}.png`，
  正是 `generate.mjs` 产出的命名
- `MINION_VARIANTS` 有 5 种，美术只产 3 套：`tank→walker`、`bomber→charger` 复用贴图，
  靠脚下光环颜色区分
- Boss 在 `spawnEnemy` 就拿到 `BOSS_BY_PLANE[plane]`；只有不在该表里的位面才回落到 `boss_{plane}.png`

`assets.js` 的预载清单必须与上面严格对齐 —— 多请求就是每局白跑 404，少请求就是满屏色块。

### 5.4 像素资产图鉴提示词（备用）

生成「一张图看完整套资产」的总览图时用，不是生产管线的一部分：

> 请根据【游戏主题】创作一张高完成度、高信息密度的「像素世界资产图鉴 / Pixel World Asset Atlas」。
> 这不是普通像素插画，也不是单张场景图，而是面向游戏开发、美术设定、关卡设计的
> 「2D 像素游戏资产总览图」，像一张完整的游戏素材说明板，把核心角色、敌人、道具、地形、
> 场景、机关、Boss 和装饰元素系统化整理在同一张图中。
> 需要填的占位符：主题世界观 / 游戏类型 / 时代背景 / 主角身份 / 敌人类型 / 主要场景 /
> 整体风格（如 16-bit 复古街机像素风）/ 主色调 / 辅助色 / 点缀色 / 画幅比例。
> 统一像素密度的 2D pixel art sprite，清晰深色描边、有限色阶、复古质感；
> 浅色或低干扰背景。不要真实摄影、3D 渲染、厚涂插画或电影海报风。

---

## 六、排查工具箱

这一节的工具都是为了回答一个问题：**东西是不是真的生效了。**
单元测试回答不了，因为它们测的是逻辑分支，不是「玩家能不能看见/摸到」。

| 工具 | 回答什么 | 出过什么事 |
|---|---|---|
| `npm run shots` | 界面好不好看；控制台报错；资源 404 的**具体 URL** | 地板是矮人立绘、敌人是色块、弹窗把同一句话列两遍 —— 全是看图才发现的 |
| `npm run audit:art` | 按渲染层「实际会请求什么」核对磁盘 | 首个位面 18 张贴图躺在磁盘上但从不加载 |
| `npm run audit:build` | 连打 N 局，统计三选一的技能/强化/属性构成 | 60 个技能是死内容（177 个选项里 0 个技能） |
| `npm run audit:enemy` | 各变体的独有行为实际触发次数 | 「加了冲刺状态机」≠「玩家会遇到冲刺」 |
| `npm run art:tile` | 地砖平铺后的样子 | 单看很漂亮的砖，铺开是棋盘壁纸 |

### 用 `npm run shots` 的注意事项

- 需要先 `npm run serve`。产物在 `.tmp/shots/`（已 gitignore）。
- 战斗截图前必须关掉「第一次苏醒」教学弹窗，否则它盖住整个战场（工具已处理）。
- **首周目进的是属性通道**，三选一必然全是属性 —— 这是设计如此，不是 bug。
  想看技能通道要预置存档（工具末尾会写 localStorage 的 geneLocks 再 reload）。
- 抬手/引信这类预警只有零点几秒，工具会放慢 timeScale 再轮询蹲拍。

### 用 `npm run audit:enemy` 的注意事项

武侠位面的变体是按 sprite 硬派的（`battle.js` 的 `spawnEnemy`），**不走权重表**，
`tank` / `bomber` 只能从涌潮路径刷出来。要看权重表的真实分布得挑别的位面：

```bash
npm run audit:enemy -- 180 jiguan
```

---

## 七、敌人行为

`MINION_VARIANTS` 五种变体，曾经只有 `spitter` 有独立 AI 分支，
其余四种跑的是完全相同的两行直线追踪 —— 差别只在 `speedMul` / `hpMul` 两个数值，
所谓「冲撞形态」只是走得快 1.9 倍。现在：

| 变体 | 行为 | 预警 |
|---|---|---|
| walker | 直线追击，构成怪海本体 | — |
| charger | 进入距离带 → 原地抬手（**方向此刻锁定**）→ 直线冲刺 → 长 CD | 收缩圈 + 指向玩家的蓄力线 |
| spitter | 停在射程边缘吐射，逼玩家主动近身 | 瞄准抬手 0.4s |
| tank | 慢而硬（**目前仍只有数值差异，待补行为**） | — |
| bomber | 贴近点燃引信，烧完原地引爆自毁；退出范围会熄火 | 越烧越快的红闪 + 爆炸半径预告圈 |

冲刺方向在**离手瞬间锁死**、冲刺期不再追踪 —— 这是关键：会拐弯的冲刺等于必中，
只有「提交后不再修正」才让走位有意义。
自爆自毁不计击杀、不给基因 —— 躲开它本身就是收益。

精英 / Boss 有 `telegraphT` 抬手 + 技能释放 + Boss 三阶段狂暴，这部分本来就有。

---

## 八、已知问题 / 待办

1. **`tank` 仍然只有数值差异**，没有独有行为。它是唯一还没做的变体。
2. **`jiguan` 不在 `MINION_SPRITE_BY_STAGE` 和 `BOSS_BY_PLANE` 里**（`npm run audit:art` 会报）。
   靠通用命名兜底能跑，但代价是首个教学位面 5 个阶段只有 3 种小怪造型，
   而其他位面有 10 种。属于内容缺口，不是 bug。
3. **4.3 的两条待策划裁定**（PRD 稀有度、后期难度旋钮）。
4. **两套 UI 的漂移风险**（见 3.1）—— 目前靠 `npm run smoke` 覆盖 Cocos 侧，
   但它只覆盖走到过的状态，新增 RunState 时仍需人工同步。
5. **`ccResist`（禅心）暂无消费者** —— 它是玩家自身的控制抗性，但游戏目前
   没有任何针对玩家的控制效果。曾误把它拿去缩短敌人被减速的时长
   （点防御技能反而削弱自己的冰霜），已修正并锁进 `tests/statusBuild.test.mjs`；
   等未来加入玩家侧控制（冰冻/眩晕）时它才有实际作用。

---

## 九、出征武器 / 已接线技能 / 共鸣扩充

> 依据「动作肉鸽设计」调研（Vampire Survivors / Brotato / Hades 流派，
> Escapist《Roguelites Have Learned the Right Ways to Scale Player Powers》、
> Jank《Death to the Self-Playing Game》）：**玩法在「选什么」而不在「叠多少」**，
> 并要警惕「自动索敌剥削里玩家只剩走位」的自玩陷阱。三处落地——

1. **出征武器（weaponLoadout）** —— 开裂缝前指定用哪条路线的武器 / 路线机制 / 皮肤，
   三选一的机制强化池也跟随出征路线。原先「武器 = 基因锁最高路线」让流派被元进度锁死，
   这是构建多样性的最大瓶颈。未选则沿用旧口径（最高路线）。锁进 `tests/statusBuild.test.mjs`。
2. **已接线但死掉的技能效果** —— `skills.js` 里声明却从未进战斗的字段（dot 持续伤害、
   elemental 元素、counter 反击、reflect 反弹、shield 护盾、ccResist、size 体型、
   dodgeAspd、killCdRefund、vsEliteDmgPct、geneBonus）此前是**零效果**的「死技能」。
   现在聚合进 `stats` 并且都在 `battle.js` 结算（DoT 逐帧止血、护盾先挡、元素减速、
   闪避后攻速窗口、击杀回 CD、对精英增伤、额外基因）。
3. **共鸣扩充** —— `checkSynergies` 现在也应用新轴（dot/elemental/shield/vsElite/killCd）。
   新增 4 条共鸣（燃域/坚垒/诛主/锯齿），把「凑套」从叠数字升级为换玩法。

> 审查备注：元素减速为固定 1.5s，且对冲撞怪的接近阶段同样生效（冲刺期免控不变）——
> 两条均有回归测试；`weaponAttack.js` 中无调用方的 `weaponForRoute`/`skinForRoute` 已删除。

# 实时战斗落地计划

> 目标：把噬祖从「回合抽象层（`core/combatModel.js`）」升级为**实时割草战斗**，
> 借鉴参考项目 [Slash The Hordes](https://github.com/MartinKral/Slash-The-Hordes) 的战斗层架构，
> **机制仍完全用噬祖自己的**（基因锁 / 三选一 / 掉落 / D 值快照 / 存档 / 九条红线）。

---

## 一、借鉴的 7 个核心模式（来自 Slash The Hordes 源码）

### 1. `gameTick(deltaTime)` 系统循环 —— 最重要
`Game.update()` 不做具体逻辑，只逐个调子系统的 `gameTick(dt)`：

```ts
this.player.gameTick(dt);
this.playerCollisionSystem.gameTick(dt);
this.enemyManager.gameTick(dt);
this.projectileLauncher.gameTick(dt);
this.itemAttractor.gameTick(dt);
```

每个系统自包含、可独立测试、按固定顺序推进。**照搬。**

### 2. 输入抽象 `IInput`
```ts
interface IInput { getAxis(): Vec2 }   // 归一化移动向量
```
`VirtualJoystic`（触摸摇杆）+ `KeyboardInput`（WASD/方向键）+ `MultiInput`（组合）。
玩家只认 `getAxis()`，不关心输入来源。**照搬，对应噬祖「单摇杆」。**

### 3. 玩家移动 + 相机跟随
```ts
node.setWorldPosition(pos + axis * speed * dt);
// 每帧：
MainCamera.node.setWorldPosition(player.node.worldPosition);
```
世界坐标移动，相机锁玩家。**照搬。**

### 4. 敌人四件套（组件 + 管理器 + 生成器 + 移动器）
- `Enemy`（组件）：`setup(位置, 配置)` / `gameTick(方向, dt)` / `dealDamage()` / 死亡事件
- `EnemyManager`：持有 spawner 列表 + mover 列表
- `CircularEnemySpawner`：`sin/cos * 半径` 绕玩家一圈刷 N 只 → **= 噬祖的「涌潮」**
- `FollowTargetEnemyMover`：`normalize(目标 - 自身)` 追玩家

**结构照搬，数值源换成 `core/dungeon.js`（见第四节）。**

### 5. 自动攻击 = 计时器 + 碰撞
- `Weapon`：`GameTimer(攻击间隔)` 到点 → 攻击
- `UpgradableCollider` + `WeaponCollisionSystem`：碰撞 → `enemy.dealDamage(damage)`

噬祖改为**自动索敌最近目标**（见第四节方案 B）。

### 6. 物理碰撞分组（`GroupType` 位标志）
```
DEFAULT=1<<0, PLAYER=1<<1, ENEMY=1<<2, WEAPON=1<<3, ITEM=1<<4,
PLAYER_PROJECTILE=1<<5, ENEMY_PROJECTILE=1<<6, MAGNET_RANGE=1<<7
```
`PlayerCollisionSystem` 按 `collider.group` 分发（撞敌人→扣血，撞道具→拾取），
带 `collisionDelay` 计时器**周期性结算**，防止瞬间重复受伤。

### 7. 拾取 / 吸取
- `Magnet`（CircleCollider2D 临时吸取范围）→ 对应噬祖「**吞噬爆发**」
- `ItemAttractor`（道具加速飞向玩家）→ 对应「**基因吸取**」

---

## 二、可原样照抄的纯逻辑基础设施类

| 类 | 作用 | 依赖 |
|---|---|---|
| `GameTimer` | 冷却计时（`gameTick` + `tryFinishPeriod`） | 零 cc |
| `Signal / ISignal` | 事件系统（死亡/升级/受伤解耦） | 零 cc |
| `ObjectPool` | 对象池（敌人/道具复用，同屏 60 必用） | cc（instantiate/Prefab） |
| `UnitHealth / UnitLevel` | 纯逻辑血条/等级 | 零 cc |

> 这 4 个可以直接移植进 `game/battle/`，逻辑与 cc 解耦，方便后续 Node 冒烟测试。

---

## 三、映射表：哪些照搬、哪些改

| Slash The Hordes | 噬祖对应 | 结论 |
|---|---|---|
| `gameTick` 系统循环 | 直接套 | ✅ 照搬 |
| `IInput` + 摇杆 | 单摇杆 | ✅ 照搬 |
| 敌人四件套 | 敌人数值从 `generateDungeon()` 取 | ⚠️ 结构照搬，数值源换成 core/dungeon.js |
| 武器碰撞攻击 | **自动索敌最近目标** | ⚠️ 它「挥砍碰撞」，我们要「自动索敌 + 投射」 |
| 物理碰撞（BoxCollider2D） | 当前全程序化 Graphics | ❌ 见第四节决策 |
| XP/gold 掉落 | 基因/装备掉落 | ⚠️ 掉落逻辑已有（core/drop.js），只接表现 |
| Magnet / ItemAttractor | 吞噬爆发 / 基因吸取 | ✅ 语义完全对应 |
| JSON 数据驱动 | core/data 配表 | ✅ 我们已有，更彻底 |

---

## 四、关键技术决策：碰撞用「距离检测」，不用物理引擎

Slash The Hordes 用 Cocos 2D 物理（BoxCollider2D + Contact2DType）。但噬祖是**全程序化 Graphics、无 prefab、无物理节点**。

| 方案 | 做法 | 代价 |
|---|---|---|
| A（照抄物理） | 玩家/敌人挂 BoxCollider2D + 分组 | 同屏 60 敌人碰撞开销大；要引入 prefab 结构，放弃纯程序化 |
| **B（距离检测，推荐）** | 圆形距离：`dist(player, enemy) < r` 结算 | 简单、贴合程序化；割草同屏大量单位更稳；O(n²) 但 60 只可接受 |

**选 B**。割草核心是「扫一片」，不是精确碰撞。距离检测 + 自动索敌最近目标，
正是 Vampire Survivors 类的实际做法，且不破坏噬祖「纯程序化、无 prefab」架构。

> 用 Spatial Hash（网格分桶）可把 O(n²) 降到近似 O(n)，同屏上限拉高时再上，先不做。

---

## 五、落地文件结构

```
shizu-cocos/assets/scripts/game/battle/
├── BattleController.ts     update → gameTick 循环（替代 GameRoot 的 step）
├── PlayerController.ts     单摇杆移动 + 闪避无敌帧 + 吞噬爆发（吸血吸取）
├── EnemySpawner.ts         从 dungeon.stages 时间轴刷怪（涌潮 = 环形一波）
├── EnemyMover.ts           追玩家（Follow 模式，含涌潮/单体型差异）
├── AutoTarget.ts           自动索敌：最近敌人 + 攻击间隔（= Weapon + GameTimer）
├── CombatSystem.ts         距离碰撞结算（玩家↔敌人 / 攻击↔敌人）
└── DropView.ts             基因/装备掉落 + 吸取飞向玩家（= ItemAttractor + Magnet）
```

配合已有的：
- `core/dungeon.js`：敌人数值（`stages[i].minion / closer` 已按 `基准 × D × 阶段 × dynFactor` 算好）
- `core/run.js`：三选一 / 结算 / 基因锁充能（复用，只把 `step()` 换成实时伤害）

---

## 六、实施步骤（分 4 阶段，每阶段可独立验证）

### 阶段 1：跑通「动」的最小闭环
- 竖立 BattleController + PlayerController + 单摇杆移动 + 相机跟随
- 验证：能满屏跑，相机锁玩家

### 阶段 2：跑通「打」的最小闭环
- 敌人生成（环形涌潮）+ 追击 + 自动索敌最近目标 + 距离伤害 + 死亡移除
- 验证：杀得死、追得上、同屏 60 不卡

### 阶段 3：接回噬祖数值与掉落
- 敌人 HP/ATK 从 `dungeon.stages` 取；击杀掉基因/装备（`core/drop.js`）
- 基因吸取飞向玩家；吞噬爆发范围吸取
- 三选一触发（复用 `run.choose`）+ 基因锁充能（复用 `run.finalize` 里的 `chargeGeneLock`）

### 阶段 4：闪避 / 无敌帧 / 手感调参
- 闪避（翻滚 + 无敌帧）+ 被命中率手感（对照 combatModel 的「≤8%」标定）
- 清理：删除 `core/combatModel.js` 及 run.js 对其引用（README 明确要求）

---

## 七、验收口径（不改九条红线）

1. 敌人数值仍严格 = `基准 × D × 阶段系数 × dynFactor`（红线 1）
2. D 仍在开副本时快照，局内 Build 不改敌人（红线 2）
3. 属性通道仍零技能、装备掉率 ×1.5（红线 3）
4. 存档仍在 finalize 末尾一次性 persist（红线 6）
5. 割草节奏仍达标：杂兵一刀死、一局 1000+ 击杀、同屏峰值 ≤ 60

> 实时战斗只替换 `combatModel.js` 的「回合交锋」抽象，meta 层（run.js / drop.js / save.js）逻辑一行不动。

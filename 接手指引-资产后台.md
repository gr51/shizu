# 《噬祖》资产配置后台 · 接手指引

> 供下一位接手"资产配置后台"工作的 AI 使用。目标：把游戏里全部资产（位面形象、敌人/Boss、技能、事件、掉落、进化路线、攻击方式……）做成**可通过后台 GUI 编辑、且运行时生效**的配置系统。

---

## 一、一句话现状（重要）

项目**已有**一个后台，且**基础链路是通的**：

```
web/admin.html (编辑界面)
   → localStorage["cfg_overrides_v1"]（保存覆盖对象）
   → web/src/config/overrides.js 的 applyConfigOverrides()
   → 直接就地改写 data/*.js 的 ESM 模块对象（const 对象属性可变）
   → web/src/main.js:15 在 boot 时调用 → 整个游戏读到改后的数据
```

**验证过**：在后台改"机关城"主题 → 刷新主游戏页，`planes.jiguan.theme` 确实变成新值。链路已打通。

---

## 二、文件清单

| 文件 | 说明 | 状态 |
|---|---|---|
| `web/admin.html` | 后台页面 + 样式 | ✅ 已扩展（17 个标签 + 美化样式） |
| `web/src/admin.js` | 后台逻辑（编辑→localStorage→导出） | ✅ 已重写为 17 标签全资产编辑器 |
| `web/src/config/overrides.js` | **运行时消费核心**：把覆盖应用到 data 表 | ✅ 已扩展 12 个维度 |
| `web/src/main.js` | 装配入口，第 15 行调用 `applyConfigOverrides()` | ✅ 未改动（本来就通） |
| `tools/verify-admin.mjs` | 自起 serve + Playwright 端到端验证脚本 | ✅ 新增，全部通过 |
| `tools/_probe-404.mjs` | 临时 404 探查脚本 | 🟡 可删 |

**入口**：`npm run serve`（默认 8123）→ `http://localhost:8123/web/admin.html`

---

## 三、目前能配置什么

后台已覆盖 **17 个标签**，基本对应 `shizu-cocos/assets/scripts/data/` 下的全部配表：

位面 / 敌人·Boss / 技能 / 隐藏技 / 路线·组合 / 共鸣 / 成就 / 传承 / 危机 / 词缀 / 属性池 / 虫巢 / 机械 / 攻击 / 变异 / 黑市 / 支线

但**注意覆盖深度不统一**：
- ✅ **展示型字段**（名称/描述/文案/主题/诗歌/Boss名/价格/奖励/间隔…）→ 已全部可编辑并运行时生效。
- ✅ **数值效果字段（`eff`）**：技能 / 共鸣 / 传承 / 精英词缀 / 属性池 / 虫巢强化 / 机械强化 7 类已开放 JSON 编辑框（失焦校验：合法写回、非法红字拒绝不落盘），经 `overrides.js` 合并进运行时数据表——已由 Playwright 断言 `dujie_1.eff = {"crit":0.42}` 端到端验证。
   - ✅ **隐藏技能效果已由并行工作接入 core**（`run.js:176` 消费刻印技 eff，数据侧 10 条已补齐）——后台「隐藏技」标签的警示文案可移除；后台改隐藏技 eff 即时生效。
- ✅ **新增条目**：技能/共鸣/传承/危机/词缀/属性池/变异/路线/组合技 九类「＋新增」即推送进运行时池；**新增位面**生成完整骨架（codex 运行时递增分配）并播种敌人阶段表/Boss/机制默认切面，应用后大厅可见可进（形象走色块回退，缺图红框提示）。黑市/支线/虫巢/成就因依赖函数字段仅支持改写。

---

## 四、如何运行与验证

```bash
# 1. 起服务（后台会开 8123）
node tools/serve.mjs

# 2. 浏览器打开
#    http://localhost:8123/web/admin.html

# 3. 端到端验证（自起 serve + 无头浏览器，无需手动开服务）
npm run verify:admin        # = node tools/verify-admin.mjs
```

验证脚本会：检查 17 标签渲染 → 位面/技能/攻击字段 → **eff 编辑（合法通过/非法拒绝）** → 应用后断言覆盖落盘 → **「保存到项目」写入 `web/src/config/overrides.data.json`**（serve 受控端点：固定路径/256KB/JSON 校验；main.js 启动双源合并 localStorage→文件，幂等可重复应用）→ 验证主游戏页运行时真实读到 theme 与 eff 新值 → 截图到 `.tmp/admin-verify.png`。脚本结束会自动删除该数据文件保持仓库干净——正式使用时在后台点「保存到项目」生成并自行提交。

---

## 五、已知缺口 / 下一步建议（按优先级）

0. **关卡编辑器（已落地的核心能力速查）**
   - 位面级配置全走 `plane.*` 字段经 overrides 生效：`stagePlan`(5阶段时长/刷怪率%/涌潮表/收尾)、`variantWeights`(怪物构成)、`eliteAffixChance/Pool/Count`(技能组合)、`spawn{x,y}`、`obstacles:[{x,y,r}]`(双向碰撞+渲染)、`triggers`(11事件×33动作:FirstBlood/EliteKill/BossKill/ChestOpen/StageClear/LowHp/TimeTick/SurgeSpawn/ShopOpen/ChoiceOpen/PlaneEnter × )
   - 新位面占位贴图：`npm run plane-assets`；分发：分享码(base64)或项目文件提交
   - 后续候选：拖拽摆放器、区块地砖、怪物技能行为键扩展
   - **触发器剧本示例**：
     - 开场礼：`[{"on":"onFirstBlood","actions":[{"type":"genes","amount":50}]}]`
     - 贪婪惩罚：`[{"on":"onChestOpen","actions":[{"type":"spawnElite","count":2}]}]`
     - 周期高压：`[{"on":"onTimeTick","every":30,"actions":[{"type":"surge","count":15},{"type":"freeze","duration":2}]}]`

1. ~~给 `eff` 效果字段补编辑 UI~~ ✅ **已完成**（7 类 eff JSON 编辑框 + 校验 + 运行时合并全链路验证）。
2. **补隐藏技能与共鸣等关系的引用校验**
   - 改 sprite 名/路线 id 后，应实时提示"该 id 是否存在于生成管线/其他表"，防悬空引用。
   - ✅ **敌人/Boss 部分已完成**：`敌人·Boss` 标签每个名字旁有实时像素预览（按渲染层约定探测 `units/<名>_walk0.png`），缺图红框 + 悬停提示缺图路径。
   - ✅ **背景/地砖/皮肤已完成**：每位面下方预览派生美术切面（`backgrounds/floor_<id>.png`、`backgrounds/plane_<XX>_<id>.png`、`units/player_<首路线>.png`），同样红框警示缺图。已验证 167 个引用（132 单位 + 35 场景/皮肤）全部命中。
   - ✅ **跨表引用一致性已完成**：新增 `tests/crossRefs.test.mjs`（7 项断言：位面↔路线双向、互斥指向、隐藏技能 route/槽位、攻击方式与 10 路线一一对应、共鸣 need 可解析、传承全覆盖），全部通过。后台改 id 若造成悬空，`npm test` 会当场红。
3. **资产完整性校验 + 预览**
   - 后台展示每位面当前 sprite 实际长什么样（复用 `assets/art/` 路径）。
   - 校验 `MINION_SPRITE_BY_STAGE` / `BOSS_BY_PLANE` 里的名字是否真的生成了对应 PNG。
4. **可选：把 fallback 升级为"生成新资产"**
   - 改 sprite 名后目前只改引用，不改生成管线 (`tools/pixel/*`)；要"新增形象"需动生成器。

---

## 六、技术约束（务必遵守）

- **核心是 `data/*.js` 配表 + `overrides.js` 运行时改写**，不是新建一套配置系统。改动要遵循这条既有机制，别另起炉灶。
- `overrides.js` 用 `assignShallow`（跳过函数字段，如 `apply`/`progress`/`cost`/`check`/`grant`），避免覆盖回调逻辑。
- 改 sprite/route/关系类字段后，务必跑 `npm test`（尤其 `planeModules.test.mjs`、`systems.test.mjs`）确认无红线破坏。
- web 与 Cocos 共用 core：改配置影响的是一切消费方，不只是 web。
- 临时文件 `tools/_probe-404.mjs` 建议清理；`verify-admin.mjs` 保留（可加进 package.json scripts）。
- 本机服务端口 8123，若被占用 `node tools/serve.mjs <port>` 可换。

---

## 七、交接自检
- [ ] `node tools/verify-admin.mjs` 全部通过
- [ ] `npm test` 全绿（177 项）
- [ ] `.tmp/admin-verify.png` 能看到后台界面
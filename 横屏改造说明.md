# 横屏改造说明

> 决策：由竖屏（640×960）改为**横屏（960×640）**。
> 依据：割草（horde-survival）品类需要更宽的视野来「看见怪海」，与参考项目
> Slash The Hordes（Cocos Creator 3.8 开源割草）一致。本作「单摇杆 + 自动索敌」下，
> 横屏无需牺牲单手操作。

---

## 一、改了什么

| 文件 | 改动 |
|---|---|
| `shizu-cocos/settings/v2/packages/project.json` | 设计分辨率 640×960 → **960×640**；`fit-height` → `fit-width`；`portrait` → `landscape` |
| `shizu-cocos/assets/scenes/Main.scene` | 本就为 960×640（Canvas 960×640、相机 orthoHeight 320），未改 |
| `shizu-cocos/assets/scripts/game/UiKit.ts` | `DESIGN` → `{width:960, height:640}`；`SAFE` → `{top:280, bottom:-280}` |
| `shizu-cocos/assets/scripts/game/GameRoot.ts` | 大厅改**双栏**（左：巢灵卡+基因锁卡；右：操作按钮）；局内改**双栏**（左：血条；右：同屏/之主；中下：日志） |
| `web/styles.css` | 验证壳 `max-width` 1180→1280、侧栏 320→340（本就响应式，改动仅对齐横屏） |

## 二、第一次在编辑器里验证

1. Cocos Dashboard → 打开 `shizu-cocos/`（版本 3.8.8）。
2. 打开 `assets/scenes/Main.scene`，**另存一次**规范化。
3. 点预览：应在**横屏 960×640** 下看到双栏大厅，无上下裁切。

> 坐标是我按 960×640 手排的，无编辑器可预览像素；若有局部越界，
> 只需微调 `GameRoot.ts` 里对应 `makeLabel/makeButton` 的 y 值，逻辑不受影响。

## 三、与竖屏决策的关系

- 策划文档 `整体策划 1.1` 原文写「竖屏（微信 + 抖音小游戏）」，本次是**实现侧裁定改横屏**，
  与「红线」无关，属品类手感决策。
- 若日后要回竖屏，改回 `project.json` + `UiKit.DESIGN` + `GameRoot.ts` 布局三处即可；
  战斗层（`core/`）零改动 —— 它不感知分辨率。

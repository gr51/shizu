# 重建说明

本次把仓库从「三套互不相干的半成品」重建为「一套照策划文档实现、且可验证的代码」。

---

## 一、删除了什么（72 个文件）

| 删除对象 | 原因 |
|---|---|
| `噬祖/原型/`（27 个文件） | `core/属性中心.js:20` 有**语法错误**（`永久攻%: 0` 未加引号），整个原型在任何 JS 引擎里都无法解析，打开即白屏；且 `dlc/游戏.js` 调用了 8 次从未定义的 `存档持久化()` |
| `shizu-cocos/assets/scripts/`（21 个文件） | `GameController/GameModel/GameRenderer` 是一个与《噬祖》无关的《以撒的结合》式双摇杆射击 Demo；`core/` + `data/` 照文档写了却**无任何代码路径引用**，从未被执行 |
| `噬祖/game/`（15 个文件） | 第三套并行实现，仅被一个 verify 脚本引用；`DropTable.js` 的 PRD 计数器每次 `new` 后即丢弃，保底完全失效 |
| 根目录 7 个脚本 | `_scan/_scan2/_inspect2/_verify/build-cocos/copy-cocos/serve-web` 全是硬编码 `E:/Project/aa/` 与 `C:/Program Files/.../msedge.exe` 的临时调试脚本；`copy-cocos.mjs` 的源目录在仓库里根本不存在 |

保留：`噬祖/*.md` 六份策划文档、`shizu-cocos/` 工程壳（并修掉了场景里指向已删组件的悬挂引用和一个节点上重复挂载的 `cc.Camera`）。

> 所有删除都可 `git checkout` 找回，GitHub 上也仍有完整副本。

---

## 二、新建了什么

一份核心逻辑，三个消费者，没有副本。

```
shizu-cocos/assets/scripts/
├── core/  *.js      ★ 唯一真源：零 DOM、零 cc 依赖
├── data/  *.js      ★ 纯配表
├── platform/        Web · 微信 · 抖音 存档适配（注入式）
└── game/  *.ts      Cocos 组件层（竖屏 640×960，程序化绘制）

tests/    65 项 ──┐
web/      DOM 壳 ─┼─→ 都直接 import 上面那份 core/，不是复制品
tools/    验证脚本┘
```

`core/` + `data/` 刻意保持 **plain JS + JSDoc**：Cocos 与浏览器都能直接吃，
无需构建步骤。一旦拆成 .ts/.js 两份就会漂移 —— 这个仓库上一版正是死于三套并行实现。

详见 `开发说明.md` 与 `shizu-cocos/README.md`。

---

## 三、实测结果

```
npm test        65 / 65 通过（core/ + data/，逐条守护九条编码红线）
npm run typecheck  tsc 零错误（Cocos 脚本层）
npm run smoke   Node 里实例化 GameRoot → 开裂缝 → 210 次交锋 → 结算 → 回巢
                存档落盘 862 字节，奇技基因锁激活，图鉴/背包/难度三个子界面全通
npm run e2e     无头 Chromium 实跑一局，控制台零报错、零资源加载失败
npm run sim 400 战力 1.000 → 6.336，通关 106 局（26.5%）
                动态难度系数自平衡在 1.234（钳制区间内）
                隐藏技能刻印 3 个，传承 13
位面平衡        战力 1 时 12 个位面通关率 10.7%~17.7%，平均抵达第 4.74~4.95 阶段
                —— 即「几乎都能打到位面之主，然后差一点」，符合设计支柱 3
```

**尚未验证**：Cocos Creator 编辑器内的实际渲染与布局（本环境无编辑器）。
组件层只做到「逻辑被真实执行」，像素层面需要你在编辑器里预览一次。

---

## 四、实现时发现的文档问题

三条已自行裁定并落地，一条留给策划（附完整数学推导）。

### 1. 敌人基准存在两张互相冲突的表 —— ✅ 已裁定：取通用基准

《数值平衡表》**三章**给的是全局通用基准（小怪 20/3、精英 150/8、之主 300/12），
**五章**又给了逐位面的另一套（机关城 20 → 巨神界 70，跨度 3.5 倍）。

**裁定依据**：两条硬规则同时指向三章 ——

> 《开发实现指南》红线 1：位面不设固有强度，所有敌人数值只由
> `基准 × D × 阶段系数 × dynFactor` 生成，**禁止按位面手写不同强度表**

> 《整体策划》3.2：位面模板不设固有难度

**实测证据**（这才是决定性的）：按五章逐位面值实现时，战力 1 的新玩家通关率为

| 位面 | 通关率 | 平均抵达阶段 |
|---|---|---|
| 机关城 | 14.7% | 4.82 |
| 奥法王国 | 3.7% | 4.59 |
| 渡劫之域 | 0.0% | 3.25 |
| 山海洪荒 | 0.0% | 2.16 |
| 巨神界 | **0.0%** | **1.45** |

而 12 个位面是**平等随机**的（整体策划 3.4：「无新手/终章之分」），玩家无从规避 ——
等于把 9/12 的开局判成必死。这正是红线 1 要禁止的情况。

改用三章的通用基准后，12 个位面通关率收敛到 **10.7%~17.7%**、
平均抵达 **第 4.74~4.95 阶段** —— 即「几乎都能摸到位面之主，然后差一点」，
正是设计支柱 3「BOSS 永远比你强一点点」。

**落地**：五章的逐位面数值保留在 `data/planes.js` 里作设定参考，
并有测试断言它们**不得流入战斗数值**。位面差异改由文档明许的三项体现：
敌人类型 / 主题机制 / 波次数 / 数量型·单体型 HP 修正（×0.75 · ×1.5）。

### 2. 硬保底机制整层缺失 —— ✅ 已裁定：补上

《整体策划》4.5 把保底写成**两条独立规则**：

> ·「PRD 伪随机：连续不触发则概率递增，消除极端脸黑」
> ·「保底机制：传承保底次数 = 1/概率 × 1.5-2」

原实现（以及重写前的三套代码）都只有第一条。已补上第二层 `PITY_CAP`，
取值来自平衡表 7.3「保底」列：普通传承 8 / 稀有传承 35 / 传说技能 350 / 隐藏技能 500。
有测试断言：即使 rng 永不命中，也必须在保底次数**当次**出货。

### 3. 基因 → 永久成长缺换算率 —— ✅ 已裁定：600 基因/次

7.2 同时给了「局内吞噬基因 ×5% 结算为永久成长」和「单局上限 +3%」，但没给换算关系。

取 **600 基因 = 1 次成长**，依据是文档自己的
「基因产出速率 40-60/分钟」×「单局 12-15 分钟」≈ 480~900 基因/局，
即典型一局兑换 1 次 —— 正好对上八章「每次通关结算战力 ≈ +1.67%」。
常量在 `balance.js` 的 `GENES_PER_GROWTH`，改一个数即可。

### 4. ⚠ PRD 参数与稀有度目标数学上不可兼容 —— 留给策划

《数值平衡表》7.4 给的 step，与 7.3 / 4.8 声称的稀有度目标偏差 3~8 倍：

| 类型 | base | step | 实际期望 | 文档声称 |
|---|---|---|---|---|
| 普通传承 | 0.20 | 0.025 | 3.9 次 | 「8 次内必出」 |
| 稀有传承 | 0.05 | 0.008 | 9.8 次 | 「30-40 次」 |
| 传说技能 | 0.005 | 0.001 | 35.7 次 | 「300 次内期望 1-2 个」 |
| 隐藏技能 | 0.001 | 0.0004 | **60.9 次** | 「**500 次内期望 1 个**」 |

**这不是简单调参能解决的 —— 线性 PRD 的期望次数与硬保底点是死锁的**：

```
期望 ≈ 0.5 × √(2π / step)
```

要让隐藏技能期望落在 500 次，需 `step ≈ 6.3e-6`，对应硬保底点约 **159,000 次** ——
那时 PRD 已形同虚设，退化成纯 0.1% 抽卡。
换句话说：**「0.1% 基础概率」+「PRD 递增」+「期望 500 次」三者无法同时成立。**

三条可选路线，需策划选一条（每条都只改常量，不动逻辑）：

| 方案 | 做法 | 后果 |
|---|---|---|
| A | 维持 7.4 的 step | 隐藏技能约 61 局出一个 —— 「禁忌级」不再稀有，与「隐藏 > 传说」的定位冲突 |
| B | 抬高基础概率至约 0.4%，step 调到 `≈1.6e-3` | 期望 ≈ 500 次，PRD 仍有效；代价是改了 4.8 表头的 0.1% |
| C | 隐藏技能**退出 PRD**，只用硬保底 | 期望 = 1/0.001 = 1000 次，硬保底 1500 兜底；最贴近「极稀有」的手感 |

**当前落地的是 A**（7.4 那张表明确标注「供编码」），真实行为已锁成测试回归基线，
换方案时测试会立刻炸出来提醒。我倾向 **C** —— 它最贴合「禁忌级 > 传说」的稀有度定位，
且不需要改任何已公布的概率数字，但这属于经济系统的手感决策，该由你定。

### 5. 两处笔误 —— ✅ 已按上下文修正

- 《开发实现指南》10.2 里 `gaoda` 的 `route` 写成了 `'gangtie'`（那是**组名**不是路线 id）。
  已按平衡表 4.8「机甲 → 禁忌·永恒高达」取 `'jijia'`。
- `tiangong` 类型是「被动」，但两份文档都建议刻印到**主动槽** `activeB`。
  照文档保留并加了注释 —— 若是笔误，改一行即可。

### 6. 横竖屏不一致 —— ✅ 已修正

《整体策划》1.1 指定**竖屏**（微信 + 抖音小游戏），
而 `shizu-cocos/settings/v2/packages/project.json` 原本是 `960×640 landscape`。
已改为 `640×960 portrait`（fit-height）。

---

## 五、实现时引入的、文档里没有的东西

只有一处，已隔离在单独文件里并标注：

`web/src/core/combatModel.js` —— 把**实时动作战斗**压成**回合交锋**的抽象层。

原因：《噬祖》设计的是实时动作肉鸽（单摇杆 + 自动索敌 + 闪避无敌帧），
文档的 HP/攻击数值是按「玩家能躲掉绝大部分伤害」标定的。
实测反推：机关城一局需约 232 次交锋，而玩家只扛得住约 20 次阶段 1 命中
⇒ **被有效命中率必须 ≤ 8%** —— 这个数字反过来正好量化了实时战斗里闪避该有多强。

该文件取 6%，留出余量让「减伤 / 吸血 / 回血」发挥作用。
移植到 Cocos 实时战斗后整个文件废弃，改由真实碰撞与无敌帧决定。
